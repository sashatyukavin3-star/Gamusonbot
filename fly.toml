app = "gamusonbot"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[env]
  PYTHONUNBUFFERED = "1"

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 256

# чтобы не засыпал
[http_service]
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1
